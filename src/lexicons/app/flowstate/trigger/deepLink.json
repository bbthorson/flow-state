{
  "lexicon": 1,
  "id": "app.flowstate.trigger.deepLink",
  "description": "Triggers when the app is opened via a URL with matching query parameters. No special permissions required.",
  "defs": {
    "main": {
      "type": "object",
      "description": "Deep link trigger configuration.",
      "required": ["event"],
      "properties": {
        "event": {
          "type": "string",
          "description": "The event name to match in the deep link URL query parameter."
        }
      },
      "permissions": []
    }
  }
}
