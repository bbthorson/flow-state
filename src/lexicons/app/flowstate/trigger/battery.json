{
  "lexicon": 1,
  "id": "app.flowstate.trigger.battery",
  "description": "Triggers when battery level or charging state changes. Requires the Battery Status API.",
  "defs": {
    "main": {
      "type": "object",
      "description": "Battery trigger configuration.",
      "properties": {
        "level": {
          "type": "number",
          "description": "Battery level threshold (0.0 to 1.0). Flow triggers when level drops below this value.",
          "minimum": 0,
          "maximum": 1
        },
        "charging": {
          "type": "boolean",
          "description": "Expected charging state. Flow triggers on transition to this state."
        }
      },
      "permissions": ["battery-status"]
    }
  }
}
