{
  "lexicon": 1,
  "id": "app.flowstate.action.webhook",
  "description": "Sends an HTTP request to an external URL. Supports template variables from the trigger context. No special permissions required.",
  "defs": {
    "main": {
      "type": "object",
      "description": "Webhook action configuration.",
      "required": ["url"],
      "properties": {
        "url": {
          "type": "string",
          "description": "The URL to send the request to. Supports {{variable}} template syntax.",
          "format": "uri"
        },
        "method": {
          "type": "string",
          "description": "HTTP method. Defaults to POST.",
          "default": "POST",
          "enum": ["GET", "POST", "PUT", "PATCH", "DELETE"]
        },
        "headers": {
          "type": "object",
          "description": "Optional HTTP headers as key-value pairs."
        },
        "body": {
          "type": "string",
          "description": "Optional request body. Supports {{variable}} template syntax."
        }
      },
      "permissions": []
    }
  }
}
